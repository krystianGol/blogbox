<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet"
        integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Madimi+One&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="/style/style.css">
    <title>BlogBox</title>
</head>

<body>
    <div class="container">
        <div class="header">
            <h1 id="app-name-header">BlogBox</h1>
            <button type="button" class="btn btn-success" onclick="window.location.href = '/new'">New Post</button>
            <hr />
        </div>
        <% if (locals.posts && posts.length> 0) { %>
            <ul id="posts-list">
                <% posts.forEach(post=> { %>
                    <li>
                        <div class="post-container">
                            <h2 id="post-title">
                                <%=post.title %>
                            </h2>
                            <p id="post-date">
                                <%=post.date %>
                            </p>
                            <p id="post-content">
                                <%=post.content %>
                            </p>
                            <p id="post-author">
                                <%=post.author %>
                            </p>
                            <form action="/edit">
                                <div class="edit-delete-btn-container">
                                    <button id="edit-btn" type="submit" class="btn btn-primary" name="postId"
                                        value=<%=post.id%>>Edit</button>
                            </form>
                            <form action="/delete?postId=<%=post.id%>" method="POST">
                                <button id="delete-btn" type="submit" class="btn btn-danger">Delete</button>
                            </form>
                            </form>

                        </div>
                        <hr />
    </div>
    </li>
    <% }); %>
        </ul>
        <% } else { %>
            <div class="empty-blog">
                <p id="message">Ready to share your voice ? Start blogging now and let your ideas shine !</p>
            </div>
            <% } %>
                </div>
</body>

</html>